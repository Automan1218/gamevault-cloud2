# .github/workflows/container-scan.yml
# ==========================================
# Container Security Scan Workflow
# For GameVault Microservices Architecture
# ==========================================

name: "Container Scan - Docker Security"

on:
  workflow_call:
    inputs:
      scan_all_services:
        description: '是否扫描所有服务'
        required: false
        default: true
        type: boolean
  push:
    branches: ['**']
  pull_request:
    branches: [prod/master_no_k8s, dev/master, main]
  schedule:
    - cron: '0 2 * * 1'  # 每周一凌晨 2 点
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  packages: write

env:
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
  IMAGE_TAG: scan

jobs:
  # ==========================================
  # Job 1: 构建 Docker 镜像
  # ==========================================
  build-images:
    name: Build Docker Images for Scanning
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        service:
          - gamevault-gateway
          - gamevault-auth
          - gamevault-shopping
          - gamevault-forum
          - gamevault-developer
          - gamevault-social

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build Common Module First
        run: |
          echo "🔨 Building gamevault-common module..."
          if [ -d "gamevault-common" ]; then
            cd gamevault-common
            mvn clean install -DskipTests -B
            cd ..
          fi

      - name: Build Service JAR
        run: |
          echo "🔨 Building ${{ matrix.service }}..."
          cd ${{ matrix.service }}
          mvn clean package -DskipTests -B
          cd ..

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./${{ matrix.service }}
          file: ./${{ matrix.service }}/Dockerfile
          load: true
          tags: ${{ matrix.service }}:scan
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Save Docker Image
        run: |
          docker save ${{ matrix.service }}:scan | gzip > ${{ matrix.service }}-scan.tar.gz

      - name: Upload Docker Image Artifact
        uses: actions/upload-artifact@v4
        with:
          name: docker-image-${{ matrix.service }}
          path: ${{ matrix.service }}-scan.tar.gz
          retention-days: 1

  # ==========================================
  # Job 2: Trivy 扫描
  # ==========================================
  trivy-scan:
    name: Trivy Container Scan
    runs-on: ubuntu-latest
    needs: build-images

    strategy:
      fail-fast: false
      matrix:
        service:
          - gamevault-gateway
          - gamevault-auth
          - gamevault-shopping
          - gamevault-forum
          - gamevault-developer
          - gamevault-social

    steps:
      - name: Download Docker Image
        uses: actions/download-artifact@v4
        with:
          name: docker-image-${{ matrix.service }}

      - name: Load Docker Image
        run: |
          docker load < ${{ matrix.service }}-scan.tar.gz

      - name: Run Trivy vulnerability scanner (SARIF)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ matrix.service }}:scan
          format: 'sarif'
          output: 'trivy-${{ matrix.service }}.sarif'
          severity: 'CRITICAL,HIGH,MEDIUM'
          scanners: 'vuln,config,secret'

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        if: always() && github.event_name != 'pull_request'
        continue-on-error: true
        with:
          sarif_file: 'trivy-${{ matrix.service }}.sarif'
          category: trivy-${{ matrix.service }}

      - name: Run Trivy vulnerability scanner (JSON)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ matrix.service }}:scan
          format: 'json'
          output: 'trivy-${{ matrix.service }}.json'
          severity: 'CRITICAL,HIGH,MEDIUM,LOW'
          scanners: 'vuln,config,secret'

      - name: Run Trivy vulnerability scanner (Table)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ matrix.service }}:scan
          format: 'table'
          exit-code: '0'
          severity: 'CRITICAL,HIGH,MEDIUM'

      - name: Parse Trivy Results
        if: always()
        run: |
          echo "## 🐳 Trivy Scan Results - ${{ matrix.service }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f "trivy-${{ matrix.service }}.json" ]; then
            CRITICAL=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="CRITICAL")] | length' trivy-${{ matrix.service }}.json)
            HIGH=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="HIGH")] | length' trivy-${{ matrix.service }}.json)
            MEDIUM=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="MEDIUM")] | length' trivy-${{ matrix.service }}.json)
            LOW=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="LOW")] | length' trivy-${{ matrix.service }}.json)
          
            echo "### 漏洞统计:" >> $GITHUB_STEP_SUMMARY
            echo "| 严重程度 | 数量 |" >> $GITHUB_STEP_SUMMARY
            echo "|---------|------|" >> $GITHUB_STEP_SUMMARY
            echo "| 🔴 Critical | $CRITICAL |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟠 High | $HIGH |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟡 Medium | $MEDIUM |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟢 Low | $LOW |" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          
            if [ "$CRITICAL" -gt 0 ]; then
              echo "⚠️ **发现严重漏洞!**" >> $GITHUB_STEP_SUMMARY
            fi
          fi

      - name: Upload Trivy Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-report-${{ matrix.service }}
          path: |
            trivy-${{ matrix.service }}.sarif
            trivy-${{ matrix.service }}.json
          retention-days: 30

  # ==========================================
  # Job 3: Docker Scout 扫描
  # ==========================================
  docker-scout-scan:
    name: Docker Scout Security Scan
    runs-on: ubuntu-latest
    needs: build-images
    if: github.event_name != 'pull_request'

    strategy:
      fail-fast: false
      matrix:
        service:
          - gamevault-gateway
          - gamevault-auth
          - gamevault-shopping
          - gamevault-forum
          - gamevault-developer
          - gamevault-social

    steps:
      - name: Download Docker Image
        uses: actions/download-artifact@v4
        with:
          name: docker-image-${{ matrix.service }}

      - name: Load Docker Image
        run: |
          docker load < ${{ matrix.service }}-scan.tar.gz

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker Scout - CVE Scan
        uses: docker/scout-action@v1
        with:
          command: cves
          image: ${{ matrix.service }}:scan
          only-severities: critical,high
          exit-code: false
          summary: true

      - name: Docker Scout - Recommendations
        uses: docker/scout-action@v1
        with:
          command: recommendations
          image: ${{ matrix.service }}:scan
          exit-code: false

      - name: Docker Scout - SBOM
        uses: docker/scout-action@v1
        with:
          command: sbom
          image: ${{ matrix.service }}:scan
          output: scout-sbom-${{ matrix.service }}.json
          format: json

      - name: Upload Scout Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scout-report-${{ matrix.service }}
          path: scout-sbom-${{ matrix.service }}.json
          retention-days: 30

  # ==========================================
  # Job 4: Snyk 扫描 (可选)
  # ==========================================
  snyk-scan:
    name: Snyk Container Scan
    runs-on: ubuntu-latest
    needs: build-images
    if: false  # 暂时禁用,需要时改为 true

    strategy:
      fail-fast: false
      matrix:
        service:
          - gamevault-gateway
          - gamevault-auth
          - gamevault-shopping
          - gamevault-forum
          - gamevault-developer
          - gamevault-social

    steps:
      - name: Download Docker Image
        uses: actions/download-artifact@v4
        with:
          name: docker-image-${{ matrix.service }}

      - name: Load Docker Image
        run: |
          docker load < ${{ matrix.service }}-scan.tar.gz

      - name: Run Snyk to check Docker image
        uses: snyk/actions/docker@master
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          image: ${{ matrix.service }}:scan
          args: --severity-threshold=high

  # ==========================================
  # Job 5: 汇总报告
  # ==========================================
  container-scan-summary:
    name: Container Scan Summary
    runs-on: ubuntu-latest
    needs: [trivy-scan, docker-scout-scan]
    if: always()

    steps:
      - name: Download all scan reports
        uses: actions/download-artifact@v4
        with:
          path: scan-reports

      - name: Generate Summary Report
        run: |
          echo "# 🐳 Container Security Scan Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Scan Date:** $(date -u)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "## 扫描完成情况" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| 扫描项目 | 状态 |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|------|" >> $GITHUB_STEP_SUMMARY
          echo "| Trivy Scan | ${{ needs.trivy-scan.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Docker Scout | ${{ needs.docker-scout-scan.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # 统计所有服务的漏洞总数
          TOTAL_CRITICAL=0
          TOTAL_HIGH=0
          TOTAL_MEDIUM=0
          
          for json_file in scan-reports/trivy-report-*/trivy-*.json; do
            if [ -f "$json_file" ]; then
              CRITICAL=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="CRITICAL")] | length' "$json_file" || echo "0")
              HIGH=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="HIGH")] | length' "$json_file" || echo "0")
              MEDIUM=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="MEDIUM")] | length' "$json_file" || echo "0")
          
              TOTAL_CRITICAL=$((TOTAL_CRITICAL + CRITICAL))
              TOTAL_HIGH=$((TOTAL_HIGH + HIGH))
              TOTAL_MEDIUM=$((TOTAL_MEDIUM + MEDIUM))
            fi
          done
          
          echo "## 🔍 Overall Vulnerability Statistics" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Severity | Total Count |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🔴 Critical | $TOTAL_CRITICAL |" >> $GITHUB_STEP_SUMMARY
          echo "| 🟠 High | $TOTAL_HIGH |" >> $GITHUB_STEP_SUMMARY
          echo "| 🟡 Medium | $TOTAL_MEDIUM |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "$TOTAL_CRITICAL" -gt 0 ]; then
            echo "⚠️ **发现 $TOTAL_CRITICAL 个严重漏洞，需要立即处理!**" >> $GITHUB_STEP_SUMMARY
          elif [ "$TOTAL_HIGH" -gt 0 ]; then
            echo "⚠️ **发现 $TOTAL_HIGH 个高危漏洞，请尽快修复。**" >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ **未发现严重或高危漏洞。**" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload Consolidated Report
        uses: actions/upload-artifact@v4
        with:
          name: container-scan-summary
          path: scan-reports/
          retention-days: 90