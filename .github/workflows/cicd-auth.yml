name: "CI/CD - Auth Service (With Common)"

on:
  push:
    branches: ['prod/master_no_k8s']
    paths:
      - 'gamevault-auth/**'
      - 'gamevault-common/**'
      - 'pom.xml'                          # ✅ 添加父 POM
      - '.github/workflows/cicd-auth.yml'  # ✅ 修正文件名
  pull_request:
    branches: ['prod/master_no_k8s']
    paths:
      - 'gamevault-auth/**'
      - 'gamevault-common/**'
  workflow_dispatch:
    inputs:
      deploy:
        description: '是否部署到EC2'
        required: true
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

env:
  SERVICE_NAME: gamevault-auth
  SERVICE_PATH: gamevault-auth
  SERVICE_PORT: 8081
  DOCKER_REGISTRY: docker.io
  IMAGE_TAG: ${{ github.sha }}

jobs:
  # ✅ 修复：添加父 POM 安装
  build-common:
    runs-on: ubuntu-latest
    name: Build Common Module

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      # ✅ 新增：先安装父 POM
      - name: Install Parent POM
        run: |
          echo "📦 Installing parent POM..."
          mvn install -N -DskipTests -B
          echo "✅ Parent POM installed"

      # ✅ 然后构建 common 模块
      - name: Build and Install Common Module
        run: |
          if [ -d "gamevault-common" ]; then
            echo "📦 Building gamevault-common..."
            cd gamevault-common
            mvn clean install -DskipTests -B
            echo "✅ Common module installed"
          else
            echo "⚠️  No common module, skipping..."
          fi

      - name: Cache Maven Repository
        uses: actions/cache/save@v3
        with:
          path: ~/.m2/repository
          key: maven-${{ github.sha }}-${{ github.run_id }}

  build-and-test:
    runs-on: ubuntu-latest
    name: Build and Test Auth
    needs: build-common

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: gamevault_test
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Restore Maven Cache
        uses: actions/cache/restore@v3
        with:
          path: ~/.m2/repository
          key: maven-${{ github.sha }}-${{ github.run_id }}

      - name: Build Auth Service
        run: |
          echo "🔨 Building Auth Service..."
          cd gamevault-auth
          mvn clean package -DskipTests -B

      - name: Run Unit Tests
        run: |
          cd gamevault-auth
          mvn test -B
        env:
          SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/gamevault_test
          SPRING_DATASOURCE_USERNAME: test_user
          SPRING_DATASOURCE_PASSWORD: test_password

      - name: Generate Test Report
        if: always()
        run: |
          echo "## Auth Service Test Results" >> $GITHUB_STEP_SUMMARY
          cd gamevault-auth
          if [ -f target/surefire-reports/TEST-*.xml ]; then
            echo "✅ Tests completed" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️  No test results found" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload JAR Artifact
        uses: actions/upload-artifact@v4
        with:
          name: auth-service-jar
          path: gamevault-auth/target/*.jar
          retention-days: 7

  build-and-deploy:
    runs-on: ubuntu-latest
    needs: build-and-test
    name: Build Docker Image and Deploy
    if: github.ref == 'refs/heads/prod/master_no_k8s' || github.event.inputs.deploy == 'true'
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Restore Maven Cache
        uses: actions/cache/restore@v3
        with:
          path: ~/.m2/repository
          key: maven-${{ github.sha }}-${{ github.run_id }}

      - name: Build with Maven
        run: |
          cd gamevault-auth
          mvn clean package -DskipTests -B

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./gamevault-auth
          file: ./gamevault-auth/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/gamevault-auth:${{ env.IMAGE_TAG }}
            ${{ secrets.DOCKERHUB_USERNAME }}/gamevault-auth:latest
          platforms: linux/amd64
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/gamevault-auth:latest
          cache-to: type=inline

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_PRIVATE_KEY }}" > ~/.ssh/ec2_key
          chmod 600 ~/.ssh/ec2_key
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to EC2
        run: |
          ssh -i ~/.ssh/ec2_key ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} bash -s << 'ENDSSH'
          cd ~/gamevault-cloud
          echo "🚀 Deploying Auth Service..."
          sudo docker pull ${{ secrets.DOCKERHUB_USERNAME }}/gamevault-auth:latest
          sudo docker compose stop auth
          sudo docker compose rm -f auth
          sudo docker compose up -d auth
          echo "⏳ Waiting for service to start..."
          sleep 30
          if sudo docker compose ps auth | grep -q "Up"; then
            echo "✅ Auth service deployed successfully"
            sudo docker compose logs auth --tail=50
          else
            echo "❌ Auth service failed to start"
            sudo docker compose logs auth --tail=100
            exit 1
          fi
          ENDSSH

      - name: Health Check
        run: |
          ssh -i ~/.ssh/ec2_key ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} bash -s << 'ENDSSH'
          echo "🏥 Performing health check..."
          sleep 10
          if curl -f http://localhost:8081/actuator/health 2>/dev/null; then
            echo "✅ Health check passed"
          else
            echo "⚠️  Health check failed"
          fi
          sleep 5
          if curl -s "http://localhost:8848/nacos/v1/ns/instance/list?serviceName=gamevault-auth" | grep -q "hosts"; then
            echo "✅ Service registered in Nacos"
          else
            echo "⚠️  Service not yet registered in Nacos"
          fi
          ENDSSH

      - name: Cleanup
        if: always()
        run: |
          ssh -i ~/.ssh/ec2_key ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} "sudo docker image prune -f" || true
          rm -f ~/.ssh/ec2_key

      - name: Deployment Summary
        run: |
          echo "## 🎉 Auth Service Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Service**: gamevault-auth" >> $GITHUB_STEP_SUMMARY
          echo "- **Image Tag**: ${{ env.IMAGE_TAG }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Port**: 8081" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY